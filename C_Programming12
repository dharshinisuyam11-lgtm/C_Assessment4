#include <stdio.h>
int main()
{
    int n, sum = 0;

    printf("Enter a number: ");
    scanf("%d", &n);
loop:
    if (n > 0)
    {
        sum = sum + (n % 10);
        n = n / 10;
        goto loop;
    }
    printf("Sum of digits = %d", sum);
    return 0;
}
