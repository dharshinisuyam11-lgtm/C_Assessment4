#include <stdio.h>
int main()
{
    int n, count = 0;
    printf("Enter a number: ");
    scanf("%d", &n);
loop:
    if (n > 0)
    {
        count++;
        n = n / 10;
        goto loop;
    }
    printf("Number of digits = %d", count);
    return 0;
}
